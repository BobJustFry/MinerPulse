# Paste into host Caddy/nginx — generated by install.sh (external-proxy mode)

{{BASE_DOMAIN}} {
  reverse_proxy {{WEB_UPSTREAM}}
}

api.{{BASE_DOMAIN}} {
  reverse_proxy {{API_UPSTREAM}}
}

admin.{{BASE_DOMAIN}} {
  reverse_proxy {{ADMIN_UPSTREAM}}
}
